# One worker process only: the dedup + delivery log is a SQLite file on a
# single volume, so a second process would keep its own state and send its own
# copy of every DM. Concurrency comes from threads instead.
web: gunicorn --workers 1 --threads 8 --timeout 60 --bind 0.0.0.0:$PORT app:app
